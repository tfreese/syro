// val repositoryUsername = providers.gradleProperty("repositoryUsername")
// val repositoryPassword = providers.gradleProperty("repositoryPassword")

// Sucht in den gradle.properties nach:
// repositoryUser
// repositoryPassword
// Alternativ: ./gradlew build -PrepositoryUsername=my-user -PrepositoryPassword=my-secret-token
val artifactoryCredentials = providers.credentials(PasswordCredentials::class, "repository").get()

credentials {
    // username = repositoryUsername.orNull
    // password = repositoryPassword.orNull
    username = repositoryCredentials.username
    password = repositoryCredentials.password
}